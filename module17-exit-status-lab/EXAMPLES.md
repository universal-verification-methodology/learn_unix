# Module 17 examples — Exit status & && / ||

Track A (real Unix). Each folder was adapted from `learn_unix_git`.

## `exit_codes/`

See [`examples/exit_codes/README.md`](examples/exit_codes/README.md) if present.

**Try:**

```bash
cd module17-exit-status-lab/examples/exit_codes
ls -la
```
