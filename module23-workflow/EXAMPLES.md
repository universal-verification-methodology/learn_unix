# Module 23 examples — Pre-push / Make / env checklist

Track A (real Unix). Each folder was adapted from `learn_unix_git`.

## `pre_push_checks/`

See [`examples/pre_push_checks/README.md`](examples/pre_push_checks/README.md) if present.

**Try:**

```bash
cd module23-workflow/examples/pre_push_checks
ls -la
```

## `scripts/`

See [`examples/scripts/README.md`](examples/scripts/README.md) if present.

**Try:**

```bash
cd module23-workflow/examples/scripts
ls -la
```
