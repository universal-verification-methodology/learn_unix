# Module 06 examples — realpath / readlink

Track A (real Unix). Each folder was adapted from `learn_unix_git`.

## `realpath/`

See [`examples/realpath/README.md`](examples/realpath/README.md) if present.

**Try:**

```bash
cd module06-realpath-resolve/examples/realpath
ls -la
```
